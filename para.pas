program ParaCuadrados;
    var 
        A: integer;
    begin
        writeln('Ingrese el valor de A: ');
        readln(a);
        if (A<=178) then
            B= a*a;
            writeln('El cuadrado de', a, 'es', b)
            begin
                if (a mod 2=0) then
                    writeln(a,' es par')
                else 
                    writeln(a, ' es impar')
            end
            else
            writeln('Numero muy grande')
            end;
    end.